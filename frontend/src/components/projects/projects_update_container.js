import { connect } from 'react-redux';
import ProjectsUpdate from './projects_update_component';

const mapStateToProps = (state) => {
  return {

  };
};

const mapDispatchToProps = dispatch => {
  return {

  };
};

export default connect(mapStateToProps, mapDispatchToProps)(ProjectsUpdate);